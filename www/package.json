{
  "name": "pem-viewer-www",
  "version": "0.1.0",
  "description": "Parse and view PEM files in the browser",
  "main": "index.js",
  "scripts": {
    "build": "export SET NODE_OPTIONS=--openssl-legacy-provider && webpack --config webpack.config.js",
    "start": "export SET NODE_OPTIONS=--openssl-legacy-provider && webpack-dev-server"
  },
  "keywords": [
    "pem",
    "viewer",
    "wasm"
  ],
  "author": "Joost Ole Seddig <mail@joostseddig.de>",
  "homepage": "https://pem.joostseddig.de",
  "dependencies": {
    "pem-viewer": "file:../pkg"
  },
  "devDependencies": {
    "copy-webpack-plugin": "^13.0.0",
    "webpack": "^5.100.2",
    "webpack-cli": "^6.0.1",
    "webpack-dev-server": "^5.2.2"
  }
}
